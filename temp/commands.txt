= LUMEN
// Run queue worker
php artisan queue:work
// Run database migrate
php artisan migrate

= GIT
// Add uncommited files to git
git add .
// Create commit with message
git commit -m "commit message"
// Push commit changes to remote
git push -u origin master

= ANGULAR
// Build and copy files to public
ng build